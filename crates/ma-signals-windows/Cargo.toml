[package]
name = "ma-signals-windows"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false
description = "Windows signal collectors: process lifecycle and package identity, audio sessions and microphone use, behind the SignalSource seam with portable fakes"

[dependencies]
ma-core-types = { path = "../ma-core-types" }
ma-signal = { path = "../ma-signal" }
serde = { workspace = true }

[target.'cfg(windows)'.dependencies]
windows = { workspace = true }
windows-core = { workspace = true }

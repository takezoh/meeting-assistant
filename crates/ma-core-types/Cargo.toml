[package]
name = "ma-core-types"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false
description = "Shared vocabulary: identifiers, session timeline arithmetic, artifact references and the error taxonomy"

[dependencies]
serde = { workspace = true }
uuid = { version = "1", features = ["v7", "serde"] }
thiserror = "2"

[dev-dependencies]
proptest = "1"
serde_json = { workspace = true }

[package]
name = "ma-detect"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false
description = "Pure, replayable meeting detector with an adapter registry and a closed outcome partition"

[dependencies]
ma-core-types = { path = "../ma-core-types" }
ma-signal = { path = "../ma-signal" }
serde = { workspace = true }
serde_json = { workspace = true }
uuid = { version = "1", features = ["v5"] }

[dev-dependencies]
proptest = "1"

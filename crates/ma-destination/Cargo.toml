[package]
name = "ma-destination"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false
description = "Export seam: destination trait, export identity and reconciliation, retry classes, backlog-capped queue and the local egress audit record"

[dependencies]
ma-core-types = { path = "../ma-core-types" }
serde = { workspace = true }
serde_json = { workspace = true }
toml = { workspace = true }
sha2 = "0.10"
hex = "0.4"

[dev-dependencies]
jsonschema = { version = "0.26", default-features = false }

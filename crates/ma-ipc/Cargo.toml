[package]
name = "ma-ipc"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

[features]
# The build channel is compiled in. A release build has no way to enable development behaviour.
development = []

[dependencies]
ma-core-types = { path = "../ma-core-types" }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
jsonschema = { version = "0.26", default-features = false }
uuid = { version = "1" }

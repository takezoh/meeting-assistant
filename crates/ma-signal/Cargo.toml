[package]
name = "ma-signal"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false
description = "Signal envelope, collector seam and replayable JSONL timelines"

[dependencies]
toml = { workspace = true }
ma-core-types = { path = "../ma-core-types" }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
jsonschema = "0.30"

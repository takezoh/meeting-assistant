[package]
name = "ma-adapter-slack"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false
description = "Service adapter table for slack; a graph sink registered only by composition roots"

[dependencies]
ma-signal = { path = "../ma-signal" }

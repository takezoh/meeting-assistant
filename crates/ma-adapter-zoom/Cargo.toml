[package]
name = "ma-adapter-zoom"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false
description = "Service adapter table for zoom; a graph sink registered only by composition roots"

[dependencies]
ma-signal = { path = "../ma-signal" }

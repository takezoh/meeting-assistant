[package]
name = "ma-session"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false
description = "Meeting-session state machine, recording mode policy and deadline semantics as a pure, total function"

[dependencies]
ma-core-types = { path = "../ma-core-types" }
serde = { workspace = true }
serde_json = { workspace = true }
